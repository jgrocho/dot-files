" Only indent a litle bit
setlocal expandtab sw=2

" Set textwidth to my persoanl favorite
setlocal tw=72

" Make ':' part of keyword, so expanding something like '\ref{fig:' with
" <C-n> is nice.
setlocal iskeyword+=:

" Turn on spelling
setlocal spell
